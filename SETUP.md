# OnlyTiler 13 — photo manager setup

The website includes `admin.html` and `/api/projects`.

Set these Vercel environment variables for the Production environment:

- `GITHUB_TOKEN` — a GitHub fine-grained token with Contents: Read and write permission for `Onlytiler-13-website`.
- `GITHUB_OWNER` — `lovepreetsinghlove201-eng`
- `GITHUB_REPO` — `Onlytiler-13-website`
- `ADMIN_PASSWORD` — choose a strong private password.

Then visit `/admin.html` to add projects from a phone.
